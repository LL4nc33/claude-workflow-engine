# Tech Stack - Rust

## Toolchain
- **Rust** stable (latest)
- **Cargo** for package management
- **rustfmt** for formatting
- **clippy** for linting

## Common Crates
- **tokio** for async runtime
- **serde** + **serde_json** for serialization
- **axum** or **actix-web** for web APIs
- **sqlx** for database access
- **tracing** for structured logging

## Testing
- Built-in `#[test]` and `#[tokio::test]`
- **proptest** for property-based testing
- **criterion** for benchmarks
- **mockall** for mocking

## Build & Deploy
- Release builds with `--release`
- **cross** for cross-compilation
- Static linking where possible
- **cargo-audit** for dependency vulnerabilities

## CI Specifics
- `cargo fmt --check` in CI
- `cargo clippy -- -D warnings` in CI
- `cargo test --all-features`
- `cargo audit` for security

---
*Auto-generated by Claude Workflow Engine CLI (Rust profile)*
