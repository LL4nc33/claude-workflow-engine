# Tech Stack - Python

## Runtime
- **Python** >= 3.11
- **Type hints** required for all public APIs

## Package Management
- **Poetry** (preferred) or **pip** + **requirements.txt**
- Use virtual environments (`venv` / `poetry shell`)
- Pin all dependencies with exact versions in production

## Framework Options
- **FastAPI** for APIs (async-first)
- **Django** for full-stack/admin
- **Flask** for lightweight services

## Testing
- **pytest** for all tests
- **pytest-cov** for coverage
- **httpx** for async API testing
- **pytest-asyncio** for async tests

## Linting & Formatting
- **ruff** for linting and formatting (replaces flake8, isort, black)
- **mypy** for type checking
- **pre-commit** hooks

## Build & Deploy
- **pyproject.toml** for project configuration
- **Docker** multi-stage builds
- **gunicorn** / **uvicorn** for production ASGI/WSGI

---
*Auto-generated by Claude Workflow Engine CLI (Python profile)*
